@extends('layouts.appmaster')
@section('title', 'Job Match: Admin')

@section('content')
<link rel="stylesheet" href="resources/style/adminPage.css">
<br>
    <div class="container">
        <div class="table-wrapper">
            <div class="table-title">
                <div class="row">
                    <div class="col-sm-5">
						<h2>User <b>Management</b></h2>
					</div>
                </div>
            </div>
            <table class="table table-striped table-hover">
                <thead>
                    <tr>
                        <th>#</th>
                        <th>Name</th>						
						<th>User Name</th>
						<th>Role</th>
                        <th>Status</th>
						<th>Action</th>
                    </tr>
                </thead>
                <tbody>
                @foreach($userList as $user)
                    <tr style="text-align: center">
                        <td>{{$user->getIdNum()}}</td>
                        <td><a href="#">{{$user->getFirstName()}}</a></td>
                        <td>{{$user->getUserCredential()->getUserName()}}</td>
                        @if($user->getUserRole() == 0)                        
                        	<td>Admin</td>
                        @else
                        	<td>User</td>
                    	@endif
                    	@if($user->isActive())
							<td><span class="status text-success">&bull;</span> Active</td>
						@else
							<td><span class="status text-danger">&bull;</span> Suspended</td>
						@endif
						<td>
							<button formaction="#" class="pause" title="Pause" data-toggle="tooltip"><i class="material-icons text-warning">&#xe14b;</i></button>
							<a href="#" class="delete" title="Delete" data-toggle="tooltip"><i class="material-icons text-danger">&#xE5C9;</i></a>
						</td>
                    </tr>
                  @endforeach 
                </tbody>
            </table>
        </div>
    </div> 

@endsection