<?php

/*
 * Bryce Schmisseur and Hermes Mimini
 * Job Match Application 2.0
 * LoginRegistrationController.php  2.0
 * Febuary 5 2020
 *
 * DataServiceInterface is used as a stucture for any of the data services to inherit
 */

namespace App\data;

interface DataServiceInterface{
    
    function __construct();
    
    /**
     * Create Object takes in an object that is then sent to the data service
     * @param $object - generic: an object Model
     */
    public function create($object);
    
    /**
     * Update user takes in an object that is then passed through to the data service
     * @param $object - generic: an object Model
     */
    public function update($object);
    
    /**
     * deleteUser takes in an id of the user to be able to remove the row from the database
     * @param $id int
     */
    public function delete(int $id);
    
    /**
     * view all users returns a list of all user models in the database
     */
    public function viewAll();
    
    /**
     * view user takes in a specific id inorder to return a full user model
     * @param $id int
     */
    public function viewById(int $id);
}